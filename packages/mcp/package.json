{
  "name": "@schemasentry/mcp",
  "version": "0.11.0",
  "description": "Model Context Protocol server for Schema Sentry - validate, audit, and scaffold schema from AI tools.",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/arindamdawn/schema-sentry.git",
    "directory": "packages/mcp"
  },
  "homepage": "https://github.com/arindamdawn/schema-sentry#readme",
  "bugs": {
    "url": "https://github.com/arindamdawn/schema-sentry/issues"
  },
  "keywords": [
    "schema",
    "json-ld",
    "structured-data",
    "seo",
    "mcp",
    "model-context-protocol",
    "ai",
    "claude",
    "cursor"
  ],
  "publishConfig": {
    "access": "public"
  },
  "dependencies": {
    "@schemasentry/cli": "workspace:*"
  },
  "peerDependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0"
  },
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts --clean --tsconfig tsconfig.build.json",
    "lint": "echo \"lint not configured\" && exit 0",
    "test": "vitest run",
    "prepack": "pnpm build"
  },
  "devDependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "@schemasentry/cli": "workspace:*",
    "tsup": "^8.3.0",
    "typescript": "^5.7.0",
    "vitest": "^3.2.0"
  },
  "bin": {
    "schemasentry-mcp": "./dist/index.js"
  }
}
