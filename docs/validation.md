# Validation

Schema Sentry validates JSON-LD for:

- Required fields
- Recommended fields
- Format checks (URL, ISO date, etc.)
- Manifest coverage

The initial CLI emits a JSON report. HTML output is planned.
